# Go
*.exe
*.test
*.out
/dist/
/bin/
vendor/

# Compiled binaries dropped at repo root by `make build` / `make build-mcp`
/narrate
/narrate-mcp

# Node / React (player/)
node_modules/
build/
.env
.env.*
!.env.example

# Editors
.vscode/
.idea/

# macOS
.DS_Store

# Audio + plan output (local-only sinks)
*.wav
/recordings/
/tmp/kokoro-*

# Python venv (kokoro subprocess runtime — per-machine)
.venv/

# TTS model weights (large, downloaded out-of-band per render/sherpa/README.md)
models/

# Claude handoff state — per-machine, regenerable; never commit
.claude/handoff/
