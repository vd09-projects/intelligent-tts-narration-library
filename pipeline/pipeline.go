// Package pipeline is the composition root — the only place that knows
// concrete adapter / intelligence / renderer / sink implementations.
package pipeline

// TODO: implement Pipeline struct + Narrate(ctx, ref, req) (sink.SinkReceipt, error) per docs/solution-phase-design.md §3.5.
