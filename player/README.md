# React reference player

Phase 5. Consumes `plan.json` + `timeline.json` emitted by the Go core; renders block-level sync UI (jump-to-source, escalate-this-block).

TODO: initialize React app (vite + ts) when phase 5 starts.
