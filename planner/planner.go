// Package planner is the intelligence-light core. Pure, no I/O. Imports
// only plan/ and the intelligence.IntelligenceAdapter interface.
package planner

// TODO: implement Plan(ctx, doc, req, intel) (plan.NarrationPlan, error) per docs/solution-phase-design.md §3.5.
